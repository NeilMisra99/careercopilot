// This is a placeholder for shared TypeScript types, interfaces, and utility functions.
// For example:

export interface User {
  id: string;
  email: string;
  name?: string;
}

export function Greet(name: string): string {
  return `Hello, ${name}!`;
}

// You can define and export any shared code here.
